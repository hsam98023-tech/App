name: Generate APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Android APK
        uses: pwa-builder/pwa-starter-action@v2
        with:
          url: 'https://dn-e-sport-clan-ff.vercel.app'

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: DN-CLAN-APK
          path: ./**/*.apk
          
